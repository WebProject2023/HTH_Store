<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <title>{% block title %}Welcome our shop!{% endblock %}</title>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css" />
    <!-- Google Fonts Roboto -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" />
    <!-- MDB -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.1/jquery.min.js" integrity="sha512-aVKKRRi/Q/YV+4mjoKBsE4x3H+BkegoM/em46NNlCqNTmUYADjBbeNefNxYV7giUp0VxICtqdrbqU7iVaeZNXA==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <!-- Custom styles -->
    <link rel="stylesheet" href="{{asset("styles/style.css")}}" />

    <script type="text/javascript">  
      function payment(){  
       alert("Payment success!"); 
      }
    

    </script> 
</head>
<body>

    <!-- Navbar -->
<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-white">
  <!-- Container wrapper -->
  <div class="container">    
    <!-- Toggle button -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent1"
    >
      <i class="fas fa-bars"></i>
    </button>

    <!-- Collapsible wrapper -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent1" >      
      <!-- Navbar brand -->
      <a class="navbar-brand mt-2 mt-sm-0" href="{{path('homepage')}}">
        <img
          src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRjLfYiW4EUa601TuGApGTpTqiF5PF3gK9N4w&usqp=CAU"
          height="20"
          alt="Logo"
          loading="lazy"
        />
      </a>
      <!-- Left links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0" >
        <li class="nav-item active">
          <a class="nav-link " href="{{path('homepage')}}">Home</a>
        </li>
        <li class="nav-item active">
          <a class="nav-link " href="{{path('app_contact')}}">Contact</a>
        </li>
          <li class="nav-item active">
          <a class="nav-link " href="{{path('app_about')}}">About Us</a>
        </li>
        {% if app.user %}
        {% if  is_granted ('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('product_show')}}">Product</a>
        </li>
        {% endif %}
       
        {% endif %}


        {# <li class="nav-item">
          <a class="nav-link" href="#">About us</a>
        </li> #}
      </ul>
      <!-- Left links -->      
    </div>
    <!-- Collapsible wrapper -->
    
    <!-- Right elements -->
    <div class="d-flex align-items-center">
      
      {% if app.user %}
          
          <a class="nav-link me-3" href="{{ path('show_cart') }}">
            <i class="fas fa-shopping-cart"></i>
          </a>
           Welcome {{ app.user.name }},
          <a class="nav-link me-3" href="{{ path('app_logout') }}">Logout</a>
        {% else %}
        
        <a class="nav-link" href="{{ path('app_login') }}">Login</a>
        
          <a class="nav-link" href="{{ path('app_register') }}">Register</a>
        
        {% endif %}

        

      <!-- Icon -->
      {# <a class="nav-link me-3" href="#">
        <i class="fas fa-shopping-cart"></i>
      </a>
      <a class="nav-link me-3" href="#">
        <i class="fas fa-sign-in-alt"></i>
      </a> #}
    </div>
    
    <!-- Right elements -->
    
  </div>
  <!-- Container wrapper -->
</nav>
{% block body %}{% endblock %} {% block javascripts %}{% endblock %}
